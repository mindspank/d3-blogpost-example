<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
    <style>
        body {
            margin: 0;
            padding: 0;
            font: 16px sans-serif;
        }
        p {
            text-align: center;
        }
        #filter {
            width: 35%;
            height: 400px;
            float: left;
        }
        ul {
            list-style: none;
            margin: 0 20px;
            padding: 0;
            border: 1px solid #e6e6e6;
        }
        li {
            cursor: pointer;
            padding: 5px;
        }
        li.S {
            font-weight: bold;
        }
        li.X {
            background: #eee;
        }
        #chart {
            width: 50%;
            height: 400px;
            float: left;
        }
        .axis text {
            font: 10px sans-serif;
        }
        .axis path,
        .axis line {
            fill: none;
            stroke: #000;
            shape-rendering: crispEdges;
        }
    </style>
  </head>
  <body>
    <div id="filter"><p>Filter by Status</p></div>
    <div id="chart"><p>Open cases by Department</p></div>
  </body>
</html>